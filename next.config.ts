import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "upload.wikimedia.org",
        pathname: "/**",
      },
      {
        protocol: "https",
        hostname: "dashboard.wikimedia.or.id",
        pathname: "/**",
      },
    ],
  },
};

export default nextConfig;
